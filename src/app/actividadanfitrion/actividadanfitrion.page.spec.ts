import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ActividadanfitrionPage } from './actividadanfitrion.page';

describe('ActividadanfitrionPage', () => {
  let component: ActividadanfitrionPage;
  let fixture: ComponentFixture<ActividadanfitrionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ActividadanfitrionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
